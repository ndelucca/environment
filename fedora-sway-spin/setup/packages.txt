# Fuente única de verdad de los paquetes dnf que instala el bootstrap.
# Lo consume una vez 03-apps.sh. Un paquete por línea; líneas vacías y las que
# empiezan con '#' se ignoran.
#
# Lo que NO está acá (tiene su propia lógica de instalación):
#   - gh                 -> 03-apps.sh (repo de terceros)
#   - Zed                -> 03-apps.sh (script de instalación oficial)
#   - c-development      -> 06-development.sh (grupo de dnf)

# Desktop / Wayland / Sway
SwayNotificationCenter
swappy
brightnessctl
# Lo referencia el config de sway/waybar pero, fuera de eso, el Spin solo lo provee
# de forma implícita — declarado acá para que el entorno sea reproducible sobre una
# base minimal. A waybar lo arranca el /usr/share/sway/config.d/90-bar.conf del Spin.
waybar
rofi
grim
slurp
playerctl
pavucontrol
blueman
wl-clipboard
swaylock
swayidle
kanshi
wlsunset
cliphist
# OSD (on-screen display) de volumen/brillo/caps-lock. No está en los repos de Fedora;
# viene del COPR erikreider/swayosd, que 03-apps.sh habilita antes de este install.
swayosd
# Portales xdg-desktop-portal: backend de screenshare (Chromium/Meet) y file
# pickers bajo Wayland. -wlr expone el screencast en compositores wlroots como sway; -gtk
# cubre file chooser y settings. El Spin los arrastra implícito; declarados para que un
# rebuild minimal mantenga screenshare/pickers funcionando.
xdg-desktop-portal-wlr
xdg-desktop-portal-gtk

# Navegadores
chromium
firefox

# GUI apps + handlers de archivos. Algunos los arrastra el Spin de forma implícita;
# declarados acá para que un rebuild minimal sea fiel. Los defaults de mime
# (imagen/pdf/video/dir) se setean con xdg-mime en 03-apps.sh; no hay
# mimeapps.list versionado (es un archivo real que el sistema reescribe).
#   loupe   -> visor de imágenes GTK4/Adwaita (reemplaza al viejo eog)
#   papers  -> visor de PDF GTK4 (antes Evince); evita abrir PDFs en Chromium
#   mpv     -> reproductor de video
#   Thunar  -> file manager (handler de inode/directory)
loupe
papers
mpv
Thunar

# Theming Qt como baseline: aunque la sesión es GTK/Adwaita, dejamos cubierto que
# cualquier app Qt (p.ej. el agente polkit del Spin, o una futura) salga dark +
# Wayland. Config declarativa en dotfiles/.config/qt6ct + environment.d/qt.conf.
qt6ct

# Herramientas CLI
# glxinfo (glx-utils): usado por el wrapper de Neovide para detectar la versión de
# OpenGL y caer a render por software en GPUs viejas (GL < 3.2).
glx-utils
gawk
unzip
curl
ripgrep
fd-find
jq
tree
htop
cowsay
fortune-mod
tmux
# fzf: fuzzy finder. Reemplaza el Ctrl-R de bash por una búsqueda difusa a pantalla
# completa sobre el historial (HISTSIZE grande) y agrega Ctrl-T/Alt-C/**<tab>. Se
# integra con fd. Cableado en dotfiles/.bashrc.d/05-fzf.sh.
fzf
# eza: ls moderno (sucesor de exa). Íconos Nerd Font (ya tenemos la fuente), colores y
# columna git. Repunta los alias ll/la/l en 00-aliases.sh; `ls` pelado queda como GNU ls.
eza
# bat: cat con resaltado de sintaxis y paginado. Suele entrar como dependencia débil;
# se declara acá para reproducibilidad. NO se aliasa a `cat` a propósito (bat pagina y
# decora; `cat` queda plano para scripts/pipes). Se usa como comando aparte.
bat
# git-delta: pager de diffs para git (core.pager). Hace git diff/show/log -p legibles
# (sintaxis, números de línea). Solo cambia cómo se VEN los diffs; se configura en
# 00-git-bash.sh (scope --system, igual que el resto de la identidad git).
git-delta

# Fuentes (parcheadas Nerd — necesarias para los glifos de íconos en waybar/foot/prompt).
# JetBrainsMono Nerd Font no está en los repos de Fedora; viene del COPR
# jhuang6451/nerd-fonts, que 03-apps.sh habilita antes de este install.
jetbrains-mono-nf
# Fallbacks de fontconfig (automáticos, sin config): JetBrainsMono no trae emoji a color
# ni glifos CJK, así que sin esto web/chat con emoji o texto chino/japonés/coreano salen
# como cajitas tofu. -color-emoji = emoji a color (no la variante monocromática);
# -sans-cjk-vf = CJK en variable font (la moderna).
google-noto-color-emoji-fonts
google-noto-sans-cjk-vf-fonts

# Editores
neovim
python3-neovim

# Lenguajes / desarrollo. Fedora no tiene un paquete 'nodejs' pelado — solo streams
# versionados — así que la versión mayor se fija explícitamente.
# MANTENIMIENTO: el stream se fija a mano. Cuando nodejs24 salga de soporte (o al subir
# de versión de Fedora) dnf falla con "no package"; bumpear acá al stream LTS vigente
# (ver `dnf module list nodejs` / `dnf list nodejs*`).
golang
nodejs24
nodejs24-npm
uv
python3-pip

# Automatización
ansible

# Linting / calidad
ShellCheck
