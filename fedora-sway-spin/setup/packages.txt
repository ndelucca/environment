# Single source of truth for dnf packages installed by the bootstrap.
# Consumed once by 03-apps.sh. One package per line; blank lines and lines
# starting with '#' are ignored.
#
# Things NOT here (they have their own install logic):
#   - gh                 -> 03-apps.sh (third-party repo)
#   - Zed                -> 03-apps.sh (official install script)
#   - c-development      -> 06-development.sh (dnf group)

# Desktop / Wayland / Sway
SwayNotificationCenter
swappy
brightnessctl
# Referenced by the sway/waybar config but otherwise only provided implicitly by
# the Spin — declared here so the env is reproducible on a minimal base.
# waybar is launched by the Spin's /usr/share/sway/config.d/90-bar.conf.
waybar
rofi
grim
slurp
playerctl
pavucontrol
blueman
wl-clipboard
swaylock
swayidle
kanshi
wlsunset
cliphist
# On-screen display for volume/brightness/caps-lock. Not in Fedora's repos; comes
# from the COPR erikreider/swayosd, which 03-apps.sh enables before this install.
swayosd

# Browsers
chromium
firefox

# GUI apps + handlers de archivos. Algunos los arrastra el Spin de forma implícita;
# declarados acá para que un rebuild minimal sea fiel. Los defaults de mime
# (imagen/pdf/video/dir) viven en dotfiles/.config/mimeapps.list.
#   loupe   -> visor de imágenes GTK4/Adwaita (reemplaza al viejo eog)
#   papers  -> visor de PDF GTK4 (antes Evince); evita abrir PDFs en Chromium
#   mpv     -> reproductor de video
#   Thunar  -> file manager (handler de inode/directory)
loupe
papers
mpv
Thunar

# Theming Qt como baseline: aunque la sesión es GTK/Adwaita, dejamos cubierto que
# cualquier app Qt (p.ej. el agente polkit del Spin, o una futura) salga dark +
# Wayland. Config declarativa en dotfiles/.config/qt6ct + environment.d/qt.conf.
qt6ct

# CLI tools
# glxinfo (glx-utils): usado por el wrapper de Neovide para detectar la versión de
# OpenGL y caer a render por software en GPUs viejas (GL < 3.2).
glx-utils
gawk
unzip
curl
ripgrep
fd-find
jq
tree
htop
direnv
cowsay
fortune-mod
tmux

# Fonts (Nerd-patched — needed for the icon glyphs in waybar/foot/prompt).
# JetBrainsMono Nerd Font isn't in Fedora's repos; it comes from the COPR
# jhuang6451/nerd-fonts, which 03-apps.sh enables before this install.
jetbrains-mono-nf

# Editors
neovim
python3-neovim

# Languages / development. Fedora has no bare 'nodejs' package — only versioned
# streams — so the major version is pinned explicitly.
golang
nodejs24
nodejs24-npm
uv
python3-pip

# Automation
ansible

# Linting / quality
ShellCheck
