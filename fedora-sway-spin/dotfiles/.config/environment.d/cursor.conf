# Tema/tamano del cursor para XWayland (y apps que leen el entorno) para que
# coincidan con el cursor de Wayland fijado via `seat seat0 xcursor_theme` en
# sway/config y con el tema GTK (Adwaita 24). environment.d de systemd lo lee al
# iniciar la sesion.
XCURSOR_THEME=Adwaita
XCURSOR_SIZE=24
