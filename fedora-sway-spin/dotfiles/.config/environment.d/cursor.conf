# Cursor theme/size for XWayland (and apps that read the environment) so they
# match the Wayland cursor set via `seat seat0 xcursor_theme` in sway/config and
# the GTK theme (Adwaita 24). Read by systemd's environment.d at session start.
XCURSOR_THEME=Adwaita
XCURSOR_SIZE=24
